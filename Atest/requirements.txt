# RAG 服务依赖
# 核心 RAG 组件
faiss-cpu>=1.7.4
# 锁定 2.7.0：3.x 用 AutoProcessor 加载 bge-small-zh-v1.5 会报错
sentence-transformers==2.7.0
rank-bm25>=0.2.2
jieba>=0.42.1

# 大模型客户端（连接本地 vLLM）
openai>=1.0.0

# 知识库构建（build_kb.py 用）
pymupdf>=1.23.0
langchain-text-splitters>=0.0.1

# 扫描版/图片版 PDF 的 OCR 回退（extract_pdf 文本不足时触发）
rapidocr_onnxruntime>=1.3.0

# HTTP 服务
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.7

# 问答缓存（Redis 后端；未配置时自动回退进程内存缓存）
redis>=5.0.0

# 间接依赖（torch/numpy 由 sentence-transformers 拉入）
numpy>=1.24.0
torch>=2.0.0
# 锁定 transformers 范围，配合 sentence-transformers 2.7.0
transformers>=4.34.0,<4.45.0
